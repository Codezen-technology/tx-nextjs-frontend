import { MinimalShell } from "@/components/layout/minimal-shell";

export default function HardcopyCertificateLayout({ children }: { children: React.ReactNode }) {
  return <MinimalShell>{children}</MinimalShell>;
}
